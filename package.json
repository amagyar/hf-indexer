{
  "name": "hf-indexer-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E tests for the Hugging Face Model Indexer frontend.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
